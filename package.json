{
  "name": "my-super-first-monorepo",
  "version": "1.0.2",
  "main": "packages/app/index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "npx lerna publish",
    "update:version": "npx lerna version --no-push"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
