{
  "private": true,
  "version": "1.0.2",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "npx lerna publish",
    "update:version": "npx lerna version --no-push"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
